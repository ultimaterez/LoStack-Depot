services:
  lenpaste:
    image: ghcr.io/lcomrade/lenpaste:1.3.1
    hostname: lenpaste
    container_name: lenpaste
    restart: unless-stopped
    networks:
    - traefik_network
    volumes:
    - ${APPDATA_DIR}/lenpaste:/data
    labels:
    - lostack.enable=true
    # - traefik.http.routers.linkding.rule=Host(`lenpaste.${DOMAINNAME}`)
    - lostack.port=80
    - homepage.name=Lenpaste
    - homepage.group=Pastebins
    - homepage.icon=mdi-clipboard-outline
    - homepage.description=Pastebin Alternative with API
    - homepage.href=https://lenpaste.${DOMAINNAME}/
    - lostack.autostart=true
    - lostack.group=lenpaste
    - lostack.default_duration=15m
    - lostack.primary=true
    - lostack.tags=notes
    - lostack.project_url=https://github.com/lcomrade/lenpaste
    - lostack.details=Lenpaste is a web service that allows you to share notes anonymously, an alternative to Pastebin. There is its own API.